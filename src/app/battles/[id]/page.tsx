import Link from "next/link";
export default function Room({params}:{params:{id:string}}){const title=params.id.split("-").map(x=>x[0].toUpperCase()+x.slice(1)).join(" ");return <main className="page"><section className="panel" style={{padding:28}}><h1 style={{fontSize:46,margin:0}}>{title}</h1><p style={{color:"var(--muted)"}}>Демо-комната баттла. Ошибки вступления больше нет.</p><div className="actions"><Link href="/battles" className="btn secondary">Назад</Link><Link href="/cases" className="btn">Добавить кейс</Link></div></section><section className="section grid">{[1,2,3,4].map(i=><div className="card" style={{padding:18}} key={i}><div className="avatar">{i===1?"T":"?"}</div><h2>{i===1?"Timon":"Свободное место"}</h2><p style={{color:"var(--muted)"}}>{i===1?"Готов к бою":"Ожидает игрока"}</p></div>)}</section></main>}
